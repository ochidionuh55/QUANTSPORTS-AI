"""Research-layer code for QUANTSPORT NEXT.

Everything here is research-only and isolated from production: it reads and
writes the research-scoped tables, never customer-facing selections, capability
gating, or the frozen production model.
"""
