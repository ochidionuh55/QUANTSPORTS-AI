# QUANTSPORT AI - development and CI dependencies
-r requirements.txt

pytest==8.3.4
pytest-asyncio==0.25.0
pytest-cov==6.0.0

# Async SQLite driver. The model, wallet and resolution tests build the full
# schema in memory so they need no live database.
aiosqlite==0.20.0
ruff==0.8.4
mypy==1.14.0
# redis>=5 ships its own type information; do not add types-redis (stale stubs).
